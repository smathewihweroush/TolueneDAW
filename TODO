File for TODO-ing stuff. TODO's are also put in files which contain something TODO, but
this is a more centralized TODO list.
Amount of exclamation marks is the priority. Pretty arbitrary but keep 3 !'s as top-priority, and 0 as very low.

smathewih, learn cpp headers and sources, and cmake, and cpp, and like anything you dont even know references and pointers and templates and
cxx stands for c cant classes :sob: :wilted_rose: :v: :im_insane:

General
- ! add logging utility
- ! add file handling utility
- ! cross-platform sleep and similar stuff
- somehow fix cmake stuff (actually learn good, modern cmake)
- add cross-platform compatability DIFFICULT

- one day, somebody shall make this look less like a joke

Audio backend
- ! is it okay if the default value for the AudioStreamOptions in openStream() is just {}?
- ! add error handling functionality (with logging utility)
- see if unique_ptr/shared_ptr may be better instead of pure pointers
- AudioBackend is structured kinda weird. how to make it better?

RtAudio backend
- !!! getAudioDevices return statement is a stub. did you write anything?
- !!! should all variables used in openStream() really be on the heap? and independent of RtAudioBackend? individual?
- !! does getStream() actually work?
- !! must check if api was started in a lot of the current functions.
- !! add full compatability with all options in RtAudio
- ! wrapping the typedefs in a struct to be able to use in the template for the rtaudiomap is so stupid.
- ! secure RtAudio instance construction
- ! do we really have to getAudioDevices() for every audio device related action?
    getAudioDevices() isn't particularly fast...
- ! probably can make all the cast functions in rtaudiomap have reference types (just add the &'s twin)